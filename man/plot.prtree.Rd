% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prtree_main_methods.R
\name{plot.prtree}
\alias{plot.prtree}
\title{Plot method for PRTree objects}
\usage{
\method{plot}{prtree}(x, y = NULL, which = NULL, ncol = NULL, ...)
}
\arguments{
\item{x}{An object of class \code{prtree}, as returned by \link{pr_tree}.}

\item{y}{Original response vector used to fit the model. Required only for
plots 1-4 (Fitted vs Observed, Residuals, Histogram, Q-Q plot).
Can be omitted if only plot 5 is requested.}

\item{which}{Which plots to produce. Options are:
\itemize{
\item \code{1}: \strong{Ovserved vs Fitted} - Scatter plot comparing
observe responses against fitted values. Requires \code{y}.
\item \code{2}: \strong{Residuals (index plot)} - Residuals plotted
against observation index. Useful for detecting patterns or outliers.
Requires \code{y}.
\item \code{3}: \strong{Histogram of residuals} - Distribution of
residuals with Freedman-Diaconis bin selection. Requires \code{y}.
\item \code{4}: \strong{Q-Q plot of residuals} - Quantile-quantile plot
to assess normality of residuals. Requires \code{y}.
\item \code{5}: \strong{Terminal node probabilities} - Boxplot of
\eqn{P(R|X)} for each terminal node. Does not require \code{y}.
}
If \code{NULL} (default), shows all plots (1-5).}

\item{ncol}{Integer. Number of columns in the plot layout. If \code{NULL} (default),
an appropriate number of columns is chosen automatically based on the number
of plots.}

\item{...}{Further arguments passed to ggplot2 (e.g., \code{color}, \code{size}, \code{alpha}).}
}
\value{
Invisibly returns the original object.
}
\description{
Provides visualization of a fitted Probabilistic Regression Tree model.
Note that the original response vector \code{y} is only required for plots 1-4.
Plot 5 (terminal node probabilities) can be generated without \code{y}.
}

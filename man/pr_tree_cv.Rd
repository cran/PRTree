% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prtree_cv.R
\name{pr_tree_cv}
\alias{pr_tree_cv}
\title{Cross-Validation for Probabilistic Regression Trees}
\usage{
pr_tree_cv(y, X, control_cv = list(), verbose = TRUE, ...)
}
\arguments{
\item{y}{A numeric vector of response values.}

\item{X}{A numeric matrix or data frame of predictor variables.}

\item{control_cv}{A list of cross-validation control parameters, typically
created by \link{pr_tree_control_cv}. Default values are taken from \link{pr_tree_control_cv}
for any parameters not specified. Alternatively, control parameters can
be passed directly via the \code{...} argument.}

\item{verbose}{Logical. If \code{TRUE} (default), prints progress and informational
messages. If \code{FALSE}, suppresses all non-error console output.}

\item{...}{Control parameters to be passed to \link{pr_tree_control}.}
}
\value{
A list of class \code{prtree.cv} with components:
\item{sigma_matrix}{Matrix of selected \eqn{\boldsymbol\sigma} values,
with one row per iteration/fold and one column per feature.}
\item{rmse_by_rep}{A list with components:
\itemize{
\item \code{validation}: Numeric vector of validation RMSE values
(NULL if no validation set used).
\item \code{test}: Numeric vector of test RMSE values
(NULL if \code{only_sigma = TRUE} or no test set).
}}
\item{track_sigma}{Logical indicating whether sigma varies across
iterations/folds (\code{TRUE}) or is fixed (\code{FALSE}). When \code{TRUE}, different
sigma values may be produced for each iteration/fold, either because they
are estimated from the data of that iteration (\code{sigma_grid = NULL}) or
selected from a grid of candidates (\code{grid_size > 1}). When \code{FALSE}, the
same sigma value is used for all iterations/folds.}
}
\description{
Performs cross-validation for Probabilistic Regression Trees. Supports
both k-fold and Monte Carlo (repeated random subsampling) methods.
}
\details{
The \link{pr_tree_cv} function provides flexible cross-validation for Probabilistic
Regression Trees, supporting three common scenarios:

\strong{1. Sigma + test error estimation (default)}

When both \code{prop_valid > 0} and a grid of \eqn{\boldsymbol\sigma} values is provided
(either via \code{control$sigma_grid} or automatically generated), the function
performs a two-step procedure in each iteration/fold:
\itemize{
\item The training data is further split into training and validation sets
according to \code{prop_valid}.
\item The validation set is used to select the optimal \eqn{\boldsymbol\sigma} from the grid.
\item The model is then refitted using the \strong{full training set} (training +
validation) with the selected \eqn{\boldsymbol\sigma}.
\item Finally, the refitted model is evaluated on the test set.
}
This yields both the selected \eqn{\boldsymbol\sigma} values for each iteration/fold and
estimates of the test error.

\strong{2. Test error estimation only (sigma vector fixed)}

When \code{prop_valid = 0} or \code{NULL}, no internal validation is performed.
This is appropriate when \eqn{\boldsymbol\sigma} is already known (e.g., from a previous
analysis) and you only need to estimate the test error. In this case:
\itemize{
\item The model is trained directly on the full training set using the
provided \eqn{\boldsymbol\sigma} values (a vector or \code{1} by \code{n_feat} matrix).
\item The test error is computed on the test set.
}
This mode is computationally lighter and focuses solely on error estimation.

\strong{3. Sigma selection only (no test error)}

When \code{prop_test = 0} (for Monte Carlo) or when you're interested only in the
selected \eqn{\boldsymbol\sigma} values and not in test error estimation, the function
can be used without a test set. In this case:
\itemize{
\item All data is used for training and validation.
\item The optimal \eqn{\boldsymbol\sigma} is selected via internal validation
(if \code{prop_valid > 0}) or using the full training set
(if \code{prop_valid = 0}).
\item No test error is computed (\code{rmse_by_rep} will contain \code{NULL}).
}
This mode is useful when you want to estimate \eqn{\boldsymbol\sigma} from the entire
dataset before refitting a final model, or when you're only interested in
the stability of \eqn{\boldsymbol\sigma} estimates across different splits.

\strong{Cross-validation methods}

Two resampling methods are available:
\itemize{
\item \strong{Monte Carlo CV} (\code{method = "montecarlo"}): In each of \code{n_iter}
iterations, the data is randomly split into training (\code{1 - prop_test})
and testing (\code{prop_test}) sets. Observations may appear in multiple
test sets across iterations.
\item \strong{k-fold CV} (\code{method = "kfold"}): The data is partitioned into
\code{n_folds} disjoint subsets. Each fold serves as the test set once,
while the remaining \code{n_folds - 1} folds are used for training and
validation. This ensures every observation is tested exactly once.
}
}
\examples{
\donttest{
# Generate example data
set.seed(123)
X <- matrix(runif(1000, 0, 10), ncol = 2)
y <- 2 * sin(X[, 1]) + 0.5 * X[, 2] + rnorm(500, 0, 0.2)

# Default k-fold CV
cv1 <- pr_tree_cv(y, X)
plot(cv1)

# Monte Carlo CV with custom parameters
control_cv <- pr_tree_control_cv(
  method = "montecarlo",
  n_iter = 20,
  prop_test = 0.3,
  prop_valid = 0.2
)
cv2 <- pr_tree_cv(y, X, control_cv = control_cv)
plot(cv2)
}

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prtree_control.R
\name{pr_tree_control_cv}
\alias{pr_tree_control_cv}
\title{Control parameters for PRTree with optional cross-validation}
\usage{
pr_tree_control_cv(method = "montecarlo", n_rep = 10, only_sigma = FALSE,
  prop_test = 0.2, prop_valid = 0.2, stratify = FALSE,
  update_final = TRUE, fold_idx = NULL, ...)
}
\arguments{
\item{method}{Character. Cross-validation method: either \code{"kfold"} or
\code{"montecarlo"}. Default is \code{"montecarlo"}.}

\item{n_rep}{Integer. Number of folds for \eqn{k}-fold CV or number of
iterations for Monte Carlo CV. Default is 10.}

\item{only_sigma}{Logical. If \code{TRUE}, performs only \eqn{\boldsymbol\sigma}
estimation without estimating test error. Default is \code{FALSE}.
\itemize{
\item When \code{sigma_grid} has multiple rows (or is \code{NULL}):
\code{only_sigma = TRUE} selects \eqn{\boldsymbol\sigma} but does not use a
test set. \code{only_sigma = FALSE} performs both \eqn{\boldsymbol\sigma}
selection and test error estimation.

\item When \code{sigma_grid} has a single row (fixed sigma):
\code{only_sigma} is automatically set to \code{FALSE}, as only test error
estimation is meaningful.
}}

\item{prop_test}{Numeric between 0 and 1. Proportion of data for the test
set in Monte Carlo CV. Default is 0.2.
\itemize{
\item For \code{method = "montecarlo"}: Defines the proportion for the test set.
If \code{0}, no test set is used (useful for \eqn{\boldsymbol\sigma} estimation
only). Ignored if \code{only_sigma = TRUE}.

\item For \code{method = "kfold"}: This parameter is ignored. The test set is
defined by the folds. To perform only \eqn{\boldsymbol\sigma} estimation
with \eqn{k}-fold, set \code{only_sigma = TRUE}.
}}

\item{prop_valid}{Numeric between 0 and 1. Proportion of the training set to
use for validation (\eqn{\boldsymbol\sigma} selection). Default is 0.2.
\itemize{
\item If \verb{> 0}: the training set is split into training (\code{1 - prop_valid})
and validation (\code{prop_valid}) sets. The optimal \eqn{\boldsymbol\sigma} is
chosen based on validation MSE.

\item If \code{0}: No validation split is created. The algorithm still
evaluates all \eqn{\boldsymbol\sigma} candidates in the grid, but selects
the optimal one based on the in-sample (training) MSE. This can lead to
overfitting.
}}

\item{stratify}{Logical. If \code{TRUE}, performs stratified sampling based on
missing values in \code{X}. Default is \code{FALSE}.}

\item{update_final}{Logical. If \code{TRUE}, updates the final model with the optimal
\eqn{\boldsymbol\sigma} after cross-validation. Default is \code{TRUE}. When \code{TRUE}
(and \code{prop_valid > 0}), after selecting the optimal \eqn{\boldsymbol\sigma}
on a validation set, the model is \strong{re-fitted} using the entire training
set of the current fold/iteration before evaluating on the test set. If
\code{FALSE}, the model fitted on the smaller training subset (used for sigma
selection) is used for evaluation, which can be faster but may yield higher
test error estimates.}

\item{fold_idx}{Integer. Optional. A numeric vector with fold numbers (from 1
to \code{n_rep}) for each observation, allowing for pre-defined folds. If
provided, \code{method} is ignored and k-fold CV is used.}

\item{...}{Control parameters for PRTree. See \link{pr_tree_control}}
}
\value{
A list of class \code{prtree.control_cv} containing merged with
cross-validation settings
}
\description{
Creates a list of control parameters for Probabilistic
Regression Trees. The base function \link{pr_tree_control} returns parameters for
tree construction. The extended function \link{pr_tree_control_cv} returns both
tree parameters and cross-validation settings.
}
\details{
\strong{Choosing the right configuration:}

\tabular{lll}{
\strong{Goal} \tab \strong{Method} \tab \strong{Parameters} \cr
Estimate sigma + test error \tab any \tab \code{prop_valid > 0}, \code{only_sigma = FALSE} \cr
Test error only (sigma known) \tab any \tab \code{prop_valid = 0}, \code{only_sigma = FALSE} \cr
Sigma selection only \tab montecarlo \tab \code{prop_test = 0}, \code{only_sigma = TRUE} \cr
Sigma selection only \tab kfold \tab \code{only_sigma = TRUE} \cr
}
}
\examples{
# Default k-fold CV
cv1 <- pr_tree_control_cv()

# Monte Carlo CV with custom parameters
cv2 <- pr_tree_control_cv(
  method = "montecarlo",
  n_iter = 20,
  prop_test = 0.3,
  prop_valid = 0.2,
  stratify = TRUE
)

}
\seealso{
\link{pr_tree} for fitting a single tree,

\link{pr_tree_cv} for cross-validation.
}

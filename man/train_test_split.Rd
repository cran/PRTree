% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prtree_split.R
\name{train_test_split}
\alias{train_test_split}
\title{Split data into training and testing sets}
\usage{
train_test_split(n_obs = NULL, prop_test = 0.2, stratify = FALSE,
  is_NA = NULL, n_rep = NULL)
}
\arguments{
\item{n_obs}{Integer. Total number of observations. Required if \code{stratify = FALSE}. Ignored when \code{stratify = TRUE} (in which case \code{n_obs} is determined
from \code{is_NA}).}

\item{prop_test}{Numeric between 0 and 1. Proportion of data to use for
testing. Default is 0.2.}

\item{stratify}{Logical. If \code{TRUE}, performs stratified sampling based on
missing values. In this case, \code{is_NA} must be provided. Default is \code{FALSE}.}

\item{is_NA}{Optional logical vector. \code{TRUE} indicates rows with missing
values. Required if \code{stratify = TRUE}. If provided when \code{stratify = FALSE},
its length must equal \code{n_obs} and missing statistics will be computed (see
details).}

\item{n_rep}{Optional integer. Number of folds for creating a
stratified or simple random \eqn{k}-fold partition. If \code{NULL} (default),
the function generates a single training/test split according to
\code{prop_test}. When specified, \code{prop_test} is ignored and the function
returns a vector of fold assignments (\code{fold_idx}) instead of training
and testing indices.}
}
\value{
An object of class \code{idx.split}.

If \code{n_rep = NULL} (default), the returned object contains:
\itemize{
\item \code{idx_train}: Integer vector of training indices.
\item \code{idx_test}: Integer vector of testing indices (or \code{NULL} if
\code{prop_test = 0}).
\item \code{n_train}: Integer. Number of training observations.
\item \code{n_test}: Integer. Number of testing observations.
\item \code{stratified}: Logical indicating whether stratified sampling was
used.
\item \code{missing_stats}: List with missing-value counts (if \code{is_NA} is
provided), containing:
\itemize{
\item \code{train_na}: Number of rows with missing values in the training set.
\item \code{test_na}: Number of rows with missing values in the testing set.
\item \code{total_na}: Total number of rows with missing values.
}
}

If \code{n_rep} is specified, the returned object contains:
\itemize{
\item \code{fold_idx}: Integer vector indicating the fold assignment of each
observation.
\item \code{n_rep}: Integer. Number of folds.
\item \code{stratified}: Logical indicating whether stratified sampling was
used.
\item \code{fold_stats}: Data frame summarizing each fold, including the number
of observations, the number of rows with missing values, and the
corresponding proportion of missing values.
}
}
\description{
Creates indices for training and testing sets. Can use simple
random sampling or stratified sampling based on missing values.
}
\details{
The function creates indices for training and testing sets using
either simple random sampling or stratified sampling based on missing
values.

\strong{Stratified sampling:}
When \code{stratify = TRUE}, the sampling preserves the proportion of rows with
missing values in both training and testing sets. In this case:
\itemize{
\item \code{is_NA} must be provided
\item \code{n_obs} is ignored (determined from \code{is_NA})
}

\strong{Missing values statistics:}
If \code{is_NA} is provided but \code{stratify = FALSE}, the function will still
compute missing values statistics for the resulting splits. The length of
\code{is_NA} is against \code{n_obs} to ensure consistency. These statistics will be
available in the \link{summary} output, with a note indicating that they were
not used for stratification.

\strong{Reproducibility:} To ensure reproducible splits, call \link{set.seed} before
using this function.
}
\examples{
\donttest{
# Simple random split
set.seed(123)
split <- train_test_split(n_obs = 100, prop_test = 0.3)
str(split)

# Stratified split by missing values
missing <- sample(c(TRUE, FALSE), 100, replace = TRUE, prob = c(0.2, 0.8))
split <- train_test_split(
  prop_test = 0.3,
  stratify = TRUE,
  is_NA = missing
)
summary(split)

# Compute missing stats without stratification
missing2 <- sample(c(TRUE, FALSE), 100, replace = TRUE, prob = c(0.2, 0.8))
split <- train_test_split(
  n_obs = 100,
  prop_test = 0.3,
  stratify = FALSE,
  is_NA = missing2
)
summary(split) # Note about missing stats will appear
}

# Stratified 10-fold partition
folds <- train_test_split(
  n_obs = 1000,
  is_NA = sample(c(TRUE, FALSE), prob = c(0.1, 0.9), replace = TRUE, size = 1000),
  stratify = TRUE,
  n_rep = 5
)
summary(folds)
}
